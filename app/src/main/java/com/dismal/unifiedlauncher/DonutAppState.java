package com.dismal.unifiedlauncher;

import android.content.Context;

/**
 * Donut launcher is self-contained (no model injection needed).
 * This class exists as a placeholder for consistency.
 */
public class DonutAppState {
    public static void init(Context context) {
        // Donut's Launcher manages its own state internally
    }
}
